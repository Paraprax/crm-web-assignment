<h1>All Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <contactentries><ul><%= contact.first_name %> <%= contact.last_name %> <email>(<%= contact.email %>)</email></ul>
   <ul><%= contact.note %></contactstyle></ul>
<% end %>
</ul>
